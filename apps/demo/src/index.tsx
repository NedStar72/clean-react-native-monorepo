import React from 'react';
import { SafeAreaView, Text } from 'react-native';

const App: React.FC = () => {
  return (
    <SafeAreaView>
      <Text>{'Hello, world!'}</Text>
    </SafeAreaView>
  );
};

export default App;
