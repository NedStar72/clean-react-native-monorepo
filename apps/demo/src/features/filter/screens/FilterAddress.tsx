import React from 'react';
import { Text } from 'react-native';

export interface FilterAddressProps {}

const FilterAddress: React.FC<FilterAddressProps> = () => {
  return <Text>{'FilterAddress'}</Text>;
};

export default FilterAddress;
